# SQL Practice - May 8, 2026

- Learned advanced SQL joins like `LEFT JOIN` and `RIGHT JOIN`.
- Learned how to use `GROUP BY` with aggregate functions like `COUNT()`, `SUM()`, and `AVG()`.
- Learned solving subquery-based problems using `IN` and nested `SELECT`.
