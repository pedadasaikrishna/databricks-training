# SQL Practice - May 7, 2026

- Learned how to use `GROUP BY` for department-wise calculations.
- Learned aggregate functions like `SUM()`, `AVG()`, `MAX()`, and `COUNT()`.
- Learned filtering grouped data using `HAVING`.
- Learned using subqueries with `IN` and `ORDER BY`.
